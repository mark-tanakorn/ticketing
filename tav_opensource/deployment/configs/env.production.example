# TAV Engine - Production Configuration
# Use this for internet-accessible deployments

# REQUIRED: Your domain name
DOMAIN=tav.example.com

# REQUIRED: Security Keys (MUST be strong and unique!)
# Generate with: openssl rand -base64 32
SECRET_KEY=CHANGE-ME-TO-STRONG-RANDOM-KEY
ENCRYPTION_KEY=CHANGE-ME-TO-32-CHAR-KEY

# CORS - Configure for your domain
CORS_ORIGINS=["https://tav.example.com","https://www.tav.example.com"]

# Base URL for email links and webhooks
BASE_URL=https://tav.example.com

# Production Settings
ENABLE_DEV_MODE=false
ENVIRONMENT=production
LOG_LEVEL=INFO

# Database
DATABASE_URL=sqlite:///./data/tav_engine.db

# Optional: Number of workers
WORKERS=2

# Optional: Custom ports (default: 5000 for backend, 3000 for frontend)
# BACKEND_PORT=5000
# FRONTEND_PORT=3000

# Optional: AI API Keys (better to configure via UI)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# DEEPSEEK_API_KEY=
# GOOGLE_API_KEY=

# Optional: For nginx reverse proxy
# HTTP_PORT=80
# HTTPS_PORT=443

