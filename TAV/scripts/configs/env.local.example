# TAV Engine - LAN Access Configuration
# Use this for local network (WiFi) development and testing

# Your local network IP address
# Find it using: ipconfig (Windows), ifconfig (Mac/Linux), or hostname -I (Linux)
LAN_IP=192.168.1.100

# =============================================================================
# PORT CONFIGURATION
# =============================================================================
# Change these if the default ports are in use by other services
# Common alternative: BACKEND_PORT=5001, FRONTEND_PORT=3001
BACKEND_PORT=5000
FRONTEND_PORT=3000

# Security Keys (auto-generate or use existing)
SECRET_KEY=dev-secret-key-change-in-production
ENCRYPTION_KEY=dev-encryption-key-change-prod-32b

# Development Mode (bypasses authentication)
ENABLE_DEV_MODE=true

# Environment
ENVIRONMENT=development
LOG_LEVEL=INFO

# Database (SQLite for simplicity)
DATABASE_URL=sqlite:///./data/tav_engine.db

# Optional: AI API Keys (better to configure via UI)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# DEEPSEEK_API_KEY=

